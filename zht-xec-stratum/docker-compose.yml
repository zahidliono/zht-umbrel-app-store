version: "3.7"

services:
  app_proxy:
    environment:
      APP_HOST: zht-xec-stratum-web
      APP_PORT: 8080
      JWT_SECRET: "${JWT_SECRET}"
    networks:
      - umbrel_main_network

  stratum:
    image: ghcr.io/zahidliono/zht-bch2-stratum:latest
    restart: unless-stopped
    stop_grace_period: 10s
    user: "1000:1000"
    environment:
      RPC_HOST: umbrel-host
      RPC_PORT: "9006"
      RPC_USER: zahidliono
      RPC_PASS: "Idd=9108"
      PAYOUT_ADDRESS: "ecash:qqjdnxm6h9q6n5rkr06lns4sustu7qympywm0kt5aa"
      STRATUM_PORT: "4583"
      DIFFICULTY: "1"
      COINBASE_MSG: "ZHT"
    extra_hosts:
      - "umbrel-host:host-gateway"
    ports:
      - "4583:4583/tcp"
    networks:
      default: {}
      umbrel_main_network:
        aliases:
          - zht-xec-stratum-web

networks:
  umbrel_main_network:
    external: true
    name: umbrel_main_network
